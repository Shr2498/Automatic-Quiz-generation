# Tests for issue 2

def validate_test_subject(test):
    """
    TODO: Check if the test is valid
    """
    ...

def test_more_than_one():
    """Check that there is at least more than 1 subject type"""
    assert len(total_tests) > 1
